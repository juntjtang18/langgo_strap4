'use strict';

/**
 * honer-title-rule controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::honer-title-rule.honer-title-rule');
